name: migu-sign

on:
  push:
  pull_request:
  watch:
    types: [ started ]
  schedule:
    - cron: 0 */12 * * *

jobs:
  migu-sign:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: sign
      
      run: |
        ./migu.sh
